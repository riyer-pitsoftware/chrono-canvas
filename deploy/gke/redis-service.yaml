apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: chronocanvas
  labels:
    app.kubernetes.io/part-of: chronocanvas
    app.kubernetes.io/component: cache
spec:
  selector:
    app.kubernetes.io/part-of: chronocanvas
    app.kubernetes.io/component: cache
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
  type: ClusterIP
